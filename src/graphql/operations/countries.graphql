query getCountries ($query: String, $region: String) {
    countries (query: $query, region: $region) {
        name
        flag
        population
        region
        capital
        alpha2Code
    }
}